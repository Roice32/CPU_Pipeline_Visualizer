Have consistent `const`s.
Have relevant access modifiers.
Clean-up #include's

Ignore invalid opcodes in DE, signal them to EX, let EX decide what to do with them based on IP.
	- more on this when I do exceptions

Start doing exception handling.

Say that a jump is made to 0x0000 and the first bit of code is found at 0xff00. Wouldn't that cause memory overflow in Memory?
	- maybe plain return 0 when asked about unused memory cells instead of adding them to the hashmap?

Does `executeModuleLogic` still need to return bool?

Make `Memory` be able to handle .vector_<num>